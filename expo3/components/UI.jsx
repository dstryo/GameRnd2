import React, { useRef, useState } from 'react';
import { Html } from '@react-three/drei/native';

import { View, StyleSheet } from 'react-native';

function UI() {
  return (
    <Html>
      <div style={styles.leftButton}>hello</div>
    </Html>
  );
}

export default UI;
